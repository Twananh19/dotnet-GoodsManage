<Window x:Class="GoodsManagement.WPF.Views.ProductManagement.ProductListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GoodsManagement.WPF.ViewModels"
        Title="Product List" Height="450" Width="800">
    <Window.DataContext>
        <local:ProductManagementViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBox Width="200" Margin="0,0,10,0" 
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     PlaceholderText="Search Products"/>
            <Button Content="Search" Command="{Binding SearchCommand}" />
            <Button Content="Add Product" Command="{Binding AddProductCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Products}" 
                  SelectedItem="{Binding SelectedProduct}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product ID" DisplayMemberBinding="{Binding ProductId}" Width="100"/>
                    <GridViewColumn Header="Product Name" DisplayMemberBinding="{Binding ProductName}" Width="200"/>
                    <GridViewColumn Header="Packaging" DisplayMemberBinding="{Binding Packaging}" Width="150"/>
                    <GridViewColumn Header="Unit" DisplayMemberBinding="{Binding UnitOfMeasurement}" Width="100"/>
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price, StringFormat=C}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>