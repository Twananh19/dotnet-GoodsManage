<Window x:Class="GoodsManagement.WPF.Views.Inventory.InventoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GoodsManagement.WPF.ViewModels"
        Title="Inventory Management" Height="450" Width="800">
    <Window.DataContext>
        <local:InventoryViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Inventory Management" FontSize="24" FontWeight="Bold" Margin="10" />

        <DataGrid Grid.Row="1" ItemsSource="{Binding InventoryItems}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Last Updated" Binding="{Binding LastUpdated}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>