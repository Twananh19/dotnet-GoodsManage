<Window x:Class="GoodsManagement.WPF.Views.ProductManagement.ProductEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GoodsManagement.WPF.ViewModels"
        Title="Edit Product" Height="400" Width="600">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock FontSize="20" FontWeight="Bold" Text="Edit Product" Margin="0,0,0,20"/>
            <Label Content="Product Name:"/>
            <TextBox Text="{Binding ProductName}" Margin="0,0,0,10"/>
            <Label Content="Packaging:"/>
            <TextBox Text="{Binding Packaging}" Margin="0,0,0,10"/>
            <Label Content="Unit of Measurement:"/>
            <TextBox Text="{Binding UnitOfMeasurement}" Margin="0,0,0,10"/>
            <Label Content="Price:"/>
            <TextBox Text="{Binding Price}" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>